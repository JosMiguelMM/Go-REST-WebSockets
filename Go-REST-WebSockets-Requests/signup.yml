info:
  name: /signup
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/signup
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "miguel.mendez@ejemplo.com",
        "password": "mi_código_secreto123",
        "name": "Miguel Mendoza"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: /login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "miguelmendezm0802@gmail.com",
        "password": "miguel1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: /login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "miguel.mendoza@ejemplo.com",
        "password": "mi_código_secreto123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
